<% entry = exhibit(entry, self) %>

<article>
  <header>
    <p>
      <time>
        <%= entry.pubdate %>
      </time>
    </p>
    <h3><%= entry.title %></h3>
  </header>
  <p><%= entry.body %></p>
  <p>
    <%= entry.render_body %>
  </p>
</article>
